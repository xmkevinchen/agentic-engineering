# Theme: ae-v1

Generated by graph-index-gen.py — do not edit.

### F-083 — AE 1.0 implementation — Phase-0 bounded landing (active)

Confirmed M5 replaces the recursive bootstrap-proof route with an ordinary
bounded landing. The current package retains the declaration/consumer repairs,
focused regressions, preregistration contracts, deterministic scaffolds, and
useful audit tools; it first hard-disables real E3 host execution and prevents
bootstrap attestations from carrying runtime or lifecycle authority.

### F-084 — AE 1.0 Contract Formation Phase (active)

Define the missing upstream half of AE 1.0: how repository facts, human intent,
assumptions, alternatives, trade-offs, and unresolved questions become the exact
Contract candidate that coverage reviews and a human may approve.

### F-085 — AE 1.0 formation-aware Skills and activation (active)

Implement the human-confirmed F-084 Contract Formation Phase across the Claude
Code-first AE runtime and user-facing Skills.

### F-088 — Phase 3a — the Kernel becomes reachable (abandoned)

The Kernel decides completion and nothing calls it. `plugins/ae/v1/bin/` is
empty, no skill, script, agent, or manifest entry reaches it, and completion is
displayed from a hand-editable markdown field that suppresses the review check
when set.
