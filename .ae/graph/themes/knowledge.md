# Theme: knowledge

Generated by graph-index-gen.py — do not edit.

### F-069 — Project knowledge graph — dynamically-maintained artifact wiki (Karpathy LLM-wiki approach) + retire mengdie (done)

Make AE dynamically organize its own project artifacts (features / discussions / plans / reviews / backlog) + project status into a **living knowledge graph**, so new work can be understood/analyzed against accumulated structure instead of re-derived each time. Apply Karpathy's LLM-wiki *pattern* (not a copy) — incremental ingest + periodic lint — landed on AE's existing architecture. Retire mengdie (the graph is meant to supersede it).

### F-070 — Retire mengdie — swap remaining read sites to the knowledge graph, remove write sites, zero-residue proof (done)

Plan 2 of F-069's two-plan arc (conclusion #8: build + prove the graph path FIRST, then remove — never simultaneous). The path is proven (F-069 archived, verdict pass); this feature removes mengdie:
- 5 remaining read sites (discuss/think/plan/review/plugin-stats `memory_search`) → the graph cold-start locate-step (analyze's Step-5 pattern)
- 6 write sites (Knowledge Capture sections) → removed (lessons live in CC auto-memory; AE relationships live in graph edges — conclusion #8 "writes migrate to edges")
- knowledge-capture-protocol.md retired; PRD archived; zero-residue grep as the deterministic AC; user prompted to drop the mengdie MCP server from ~/.claude.json (user-level config, not repo)

### F-071 — /ae:knowledge-refresh — manually-triggered knowledge-graph re-sync (correction channel; first run = full build) (done)

User insight (2026-07-04): F-069's write point fires incrementally at archive time — an
EXISTING project (like AE itself: 69 features) never gets a graph without a bootstrap
entry. The AE dogfood run (2026-07-04: 71 mechanical + 44 semantic edges, live prototype)
is this skill's blueprint, including its landmines: frontmatter append must preserve
newlines; source line numbers must compensate for the lines the edges themselves add;
dangling targets (F-047) must be lint-caught not hand-trusted; orphans with no grounded
relationship stay orphans (never invent edges).

### F-072 — Knowledge graph: add code/module/architecture nodes so the graph explains the SYSTEM, not just the process (done)

The current graph's nodes are all process artifacts (features / backlog / discussions).
It cannot answer "what is this piece of code, what depends on it, where does it sit in
the architecture" — the user's review of the first bootstrapped graph: 纯 artifact,
没有任何代码、必要的架构图,太表面了. This feature adds the system layer. See
`BL-187.md` for the captured design questions and `analysis.md` for the research.
The synthesis-page writer flow ships inside F-071's /ae:knowledge-refresh skill (its
Synthesis-pages section), extending the manual re-sync channel rather than adding a new one.

### F-073 — Repo-wide jargon governance — clean the stock, gate the flow (done)

Everything that enters the repo (skill prose, code comments, commit messages,
tests, docs) must carry no reviewer-attribution bookkeeping and no internal
codenames beyond a terse provenance cite. Two halves per the user ruling:
clean the existing stock, then gate the flow so it cannot regrow.

### F-075 — Graph semantic judgments: never self-judged — independent agent minimum, cross-family preferred (done)

User ruling (2026-07-04, during the Loom field test): the knowledge graph's
LLM judgments were Claude-writes/Claude-judges/Claude-re-judges — one family,
three passes. Every semantic judgment (edge candidates, batch re-judging,
synthesis-page gates, archive-time edges) now requires a judge INDEPENDENT of
the producer — a fresh-context agent at minimum, cross-family whenever
available. This applies F-074's grounded-verification posture to the graph's
own write points. Mechanical checks are unaffected.

### F-076 — Knowledge topology — complete the Karpathy pattern in one pass (active)

The synthesis layer shipped nodes without the network. The source pattern's
core is cross-referenced pages forming a topology (its lint names orphan
pages, missing cross-references, contradictions, superseded claims, missing
pages), and F-072's leaf-only v1 deferred exactly that. One pass closes the
full gap table: syn↔syn structural edges, F↔syn documented_by, cross-domain
traversal, the five semantic lint classes, and the docs/ rendering split
(human-facing architecture docs from the topology; .ae graph holds the
topology itself). No further slicing of the source pattern.
