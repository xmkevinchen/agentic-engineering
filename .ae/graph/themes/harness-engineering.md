# Theme: harness-engineering

Generated by graph-index-gen.py — do not edit.

### F-031 — cross-family failure-state not persisted — proxy 'unavailable' is only an in-flight SendMessage to TL, lost on detached/compaction; make disk-durable (done)

See `BL-110.md` for the original capture. Analysis in `analysis.md`.

### F-032 — Add `paused` as a first-class feature status — deferred-but-not-abandoned work (done)

Direct free-text feature (no origin BL). Motivation: real projects always have features
deliberately set aside for now — not active, not done, not abandoned. F-029 is the first
real instance (currently `status: parked` in `features/active/`, which readers warn+skip as
an unknown enum). See `analysis.md`.

### F-034 — Wire lint + typecheck consumers into /ae:work pre-commit chain (Checks C.1 + C.2) (done)

Promoted from BL-045 (2026-04-23, P2) via `/ae:roadmap` batch-approval 2026-05-31.

### F-035 — /ae:roadmap section (a) CLOSE verdict — auto-surface expired/superseded BLs (done)

Promoted from BL-094 (2026-05-22, P2, size S) via `/ae:roadmap` batch-approval 2026-05-31.

### F-036 — 改进 /ae:discuss:TL 主持纪律 + 产出讲人话(流程骨架不动) (done)

Promoted from BL-125 (2026-06-03). 见 `BL-125.md` 原始 capture + `analysis.md`。
Next: `/ae:discuss .ae/features/active/F-036-ae-discuss-tl-moderation-output/`(用 discuss 修 discuss,dogfood)。

### F-037 — 把 F-036 的原则(用户面 plain-language steering readout / TL 按利害×可逆性 triage / 按复杂度定深浅、砍 ceremony)扩充到 AE 其他 skills(review/plan/work/analyze/think 等) (done)

Promoted from BL-129 (2026-06-03). 见 `BL-129.md` 原始 capture + `analysis.md`。

### F-038 — /ae:discuss Round 0 「revise→强制重跑」环是 over-process —— 允许 TL 整合收敛反馈后直接推进 (done)

Promoted from BL-126 (2026-06-03, /ae:roadmap batch, size 预批 S)。见 `BL-126.md` + `analysis.md`。

### F-039 — Skill step-progress 任务跟踪被 TeamCreate/TeamDelete 切上下文搞孤立 (done)

Promoted from BL-127 (2026-06-03, /ae:roadmap batch, size 预批 XS)。见 `BL-127.md` + `analysis.md`。

### F-040 — Run-after monitor: /ae:discuss Steering Readout triage-quality (no-gate falsifiable signal) (active)

Promoted from BL-128 (2026-06-03)。见 `BL-128.md`(完整 spec)+ `analysis.md`。

### F-041 — Iterative Engage loop — discuss ⇄ plan ⇄ work should be a proactive cycle, not linear user-triggered sequence (done)

**Shipped 2026-06-20 (discussion 001 — harness-driven verification; verdict pass)**: a per-AC `verify_by`+`fixture` verification harness threaded through the existing pipeline (plan/analyze/plan-review/work/review/retrospect), gated on existing gates, no new engine. 6 commits on `feature/F-041-flow-controller`; review.md verdict pass.

### F-042 — /ae:backlog 编号分配器看不见已 promote 的 BL → 编号复用碰撞(实锤:BL-085、BL-097 各有两个不同 item) (active)

Promoted from BL-130 (2026-06-03, P2). 见 `BL-130.md`(实锤证据 + 修法)与 `analysis.md`。

### F-043 — Proxy protocol compliance — codex effort launch-layer default + structured shutdown_response discipline (done)

Multi-BL consolidation promote (2026-06-03): BL-134(P3, shutdown_response prose 不终止)+ BL-135(P2, codex effort prose 链断裂 + 静默死亡可观测性)。两者同属 Haiku proxy 协议合规层,改动文件高度重叠(codex-proxy.md / gemini-proxy.md / agent-teams canonical / plugin.json),合并交付。

### F-044 — /tmp/ae-session-check.log lifecycle policy — session id leak + indefinite accumulation (done)

Promoted from BL-088 (2026-06-17, P2). See `BL-088.md` for the full spec and `analysis.md`.

### F-045 — /ae:plan SKILL.md still references '3 Doodlestein' — cross-skill consistency cleanup after F-026 (done)

Promoted from BL-132 (2026-06-17, P3). See `BL-132.md` for the full spec and `analysis.md`.

### F-046 — check-shutdown-canonical.sh negative test — prove it exits 1 when a sentinel is injected (done)

Promoted from BL-136 (2026-06-17). See `BL-136.md` for the full spec and `analysis.md`.

### F-048 — Proactive pipeline orchestration — auto-advance + leash on the harness gates (done)

**Shipped 2026-06-21 (review verdict PASS after one fixup round)** — the MVP back-half: a **harness-driven loop** folded into `/ae:work` (no flag, no new skill): if the plan carries a harness (`verify_by` ACs or a `test.command`), work loops `review→fixup` to `verdict: pass` or cap. Deterministic skeleton = `loop-decide.sh` + `parse-review-verdict.sh` (tested); AE got its own `test.command` (the L1 oracle). On branch `feature/F-048-proactive-loop` (off F-041; UNMERGED — depends_on F-041, merge F-041 first).

### F-049 — LLM-driven verification harness — verification itself LLM-driven (code/data/domain/BDD) (done)

The piece F-041 + F-048 under-delivered: the **verification layer itself becoming LLM-driven**. F-041 gave a human-authored, slot-based `verify_by` enum; F-048 gave an LLM-driven *loop* around it. F-049 makes the **oracle** LLM-driven — the LLM analyzes a feature and determines + generates + runs its verification across code (TDD), behavior (BDD), business data, and domain invariants — not a fixed enum, not a single `test.command`.

### F-050 — Declarative task-DAG harness: per-node delivery contracts + explicit termination + autonomous loop (no unjustified pause) (done)

Free-text feature (direct user request, 2026-06-23), motivated by the v0.11.0 dogfood on a real external project (gamelab/Pong Godot+website). See `.ae/monitoring/pong-validation-findings.md` (F1/F2/F3 + proposed direction).

### F-074 — Judge-AC rubrics must demand ground-truth re-derivation, not form compliance (done)

The corpus audit proved the gap: all four synthesis pages passed the form rubric
(anchored? non-restated?) while carrying confident-but-wrong claims; a
ground-truth pass — the judge independently reads the sources, forms its own
understanding, then compares — corrected every page. The rubric TEXT sets the
judge's verification strength. This feature changes the verify_by: judge
contract so rubrics state what the judge must RE-DERIVE, with cross-family
judges preferred for self-authored content. The execution contract extends
F-065's Check 7 — the same isolation discipline its deterministic-AC coverage
judge already uses, applied to the judge-AC path one bullet down.
