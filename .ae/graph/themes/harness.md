# Theme: harness

Generated by graph-index-gen.py — do not edit.

### F-051 — Harness: LLM-authored per-node check code + storage/invocation schema (done)

Inbound: relates_to F-052, relates_to F-054, relates_to F-059

### F-052 — Harness: cross-family review of LLM-authored checks (theater guard) (done)

### F-053 — Harness: subagent-per-auto-node execution (structural pause-authority) (active)

### F-054 — Harness layer-1: LLM-driven task decomposition into a parallel DAG (per-node deliverable + harness) (done)

Inbound: relates_to F-055, relates_to F-056

### F-055 — Harness: thin DAG driver — automate the deterministic orchestration (shrink the ignition turtle) (done)

Inbound: relates_to F-056

### F-056 — Harness: F-054 Phase-3 — may_expand dynamic DAG growth (harness-gated) (active)

status: abandoned
abandoned: 2026-06-23
abandoned_reason: "dynamic-growth (expand-check) reverted as premature per F-057 audit; revisit only on real long-task need"

### F-057 — Audit: how much debt/屎 did the F-051~F-056 harness arc add? (done)

Inbound: relates_to F-056, relates_to F-058

### F-058 — AE re-plan Phase 5: LLM-dynamic orchestration + arbiter wiring + consolidation (done)

### F-059 — Harness-driven development (HDD) — LLM-driven, dynamic per-feature harness (done)

> **Industry-grounded (2026)**: "agent harness" = the scaffolding + execution/verification loops around an LLM ("harness engineering", formalized Feb 2026; "loop engineering" June 2026). The classic "acceptance test harness" already means "verifies the system meets agreed acceptance criteria." **AE's differentiator**: the harness is **LLM-driven + dynamic per-feature** (the agent authors/spikes/evolves the per-feature harness), made the spine of a full dev methodology (analyze→discuss→plan→work→review) with **two-layer independent verification**. Honest: LLM-constructed/runtime-adapted harnesses exist in research; AE's distinctive synthesis is harness-driven *development*, not first-to-LLM-author. AE implements it **under the single Claude Code session for now** (the concept also spans distributed/CI-scale + local-multi-agent practices).

Inbound: relates_to F-061, relates_to F-065

### F-060 — Supervisor agent — external watchdog (the autonomy keystone) (abandoned)

Inbound: relates_to F-061

### F-061 — AE self-bootstrap gap — make the harness mandatory + engaged for every AE feature (a present-but-optional harness is no harness) (done)

Inbound: relates_to F-062

### F-062 — Run-bind the LOOP_ITER engagement marker (plan-content-hash) so a stale marker can't false-pass the F-061 gate (done)

### F-063 — Front-load verification-method selection across the pipeline + explicit per-stage DoD (done)

Inbound: relates_to F-059, relates_to F-069

### F-065 — LLM-driven verification layer — mechanism fixed, per-feature check LLM-generated (done)

> Follow-on to the now-done **F-059** (HDD MVP). F-059 shipped static verification scripts; loom's v0.12 field-test proved they are the **wrong shape**. This feature reworks HDD's verification layer to the principle the user has stated repeatedly: **机制写死 + 脚本 LLM 动态生成** ([[feedback-llm-driven-over-hardcoded]]).

Inbound: relates_to F-066, relates_to F-067, relates_to F-069, relates_to F-074

### F-066 — AE v0.12.x dogfood-feedback response (umbrella) (done)

> Collects **all** feedback from the v0.12.x field-tests — **loom** (`docs/loom-dogfood-feedback-v0.12.md`) + **Games/tetris** (session 98971aa8, doc incoming) — into one place, names the unifying thesis, and **sequences** the work. Individual workstreams spin into their own plans/BLs per the sequencing in `analysis.md`.

Inbound: relates_to F-067, relates_to F-068

### F-067 — Harness-derived ceremony right-sizing — review lenses/depth derived from AC dimensions + verify_by mix, deterministic risk-floor + always-on generalist adversarial floor (NOT a parallel engine) (done)

> F-066 Wave 2 sibling (the ceremony half; the verify half = F-065, now done). Converged design lives in
> `.ae/features/active/F-066-dogfood-feedback-v012-response/discussions/001-llm-dynamic-redesigns/conclusion.md` (Topic 2 + Doodlestein refinements).

Inbound: relates_to F-063, relates_to F-066, relates_to F-068, relates_to F-069

### F-068 — Review scale-fit finish — standalone review human-gate (no premature archive) + soft-add lens negative-example calibration (done)

Two `review/SKILL.md` scale-fit residues, both surfaced by the Games/tetris v0.12 dogfood, both
left over after **F-067** (harness-derived ceremony right-sizing) shipped the lens-selection mechanism.
Bundled because they share one file and one theme: making `/ae:review` fit a solo/light-ceremony repo
without softening the gate.

Inbound: relates_to F-066
