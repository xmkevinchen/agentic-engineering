---
id: roadmap-crud-subcommands-specified
target: ae:roadmap
layer: 1
source: manual
---

## Context

Phase B adds three mid-sprint grooming subcommands (move/add/remove) plus one refinement command (size) to ae:roadmap. Each must specify: flow steps, scope-lock rules (require `--reason` when source or target is an active sprint for move/add/remove; NO scope-lock for size — it's refinement), symmetric Notes logging with structured bullet format.

## Prompt

Verify the ae:roadmap SKILL.md Subcommands section specifies move, add, remove, and size subcommands with correct scope-lock semantics and Notes logging.
