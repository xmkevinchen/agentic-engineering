.DS_Store
*.swp
*.swo
*~
node_modules/
# dist/ — committed for plugin distribution (no build step on install)
.claude/settings.local.json
.claude/cross-family-status.json
.claude/worktrees/

# Embedding model cache (from mengdie/second-brain MCP)
.fastembed_cache/

# Process artifacts (discussions, plans, reviews, analyses, etc.)
.ae/

# Local-only references (kept out of repo)
docs/references/pipeline-quality-failure-cases.md
docs/references/harness-engineering.md
docs/references/nyk-framework.md

# Local-only decision records (process artifacts; kept out of repo)
docs/decisions/

# Claude Code convention: contributor-local memory file (loaded alongside CLAUDE.md
# but never committed). AE uses this for AE-internal process detail (GTD model,
# feature dir layout, frontmatter schemas, autonomy boundary).
CLAUDE.local.md
