# docs/history — the eras, in reading order

Everything here is retained history: accurate about its own time, authority
over nothing. Each document carries its own status banner; the tiebreak rule
lives in [`../README.md`](../README.md).

| Era | Where | What it is |
|---|---|---|
| 4 · v1 as designed and archived | [`v1/`](v1/README.md) | The consolidated design, plan, acceptance criteria, loop graph, and dispositions. Built to 531 assertions, then archived by its own signed decision — tag `v1-kernel-archive`. The counter-example the rewrite was measured against. |
| 3 · how v1's spec was formed | [`ae-v1-design-history/`](ae-v1-design-history/README.md) | Three independent proposals and their cross-review. |
| 2 · the frozen 1.0 spec | [`../references/finalized/`](../references/finalized/README.md) | Left under references/ — a frozen spike schema references its path. Demoted long before the rest. |
| 1 · earlier artifacts | [`L-feature-gate.md`](L-feature-gate.md) · [`loom-dogfood-feedback-v0.12.md`](loom-dogfood-feedback-v0.12.md) · [`../prd/archive/`](../prd/archive/) | Feature-gate design, an early dogfood report, a retired feature PRD (path pinned by a suite test). |

Why history is kept at all: the 8,000-line prose layer being deleted encodes
one real incident per rule. Deletion does not destroy that — git history and
these documents are the index — and a rule returns only as structure or spec
when something real hits it again.
